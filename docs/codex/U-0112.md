You are working in the repo root.

Ticket: U-0112 — Web User: Autocomplete Typeahead (calls autocomplete-service)

Goal
- Ensure web-user sends autocomplete requests to the dedicated Autocomplete Service.
- Fix env keys and API wiring so Network shows GET /autocomplete calls while typing.

Constraints
- Only modify apps/web-user/**.
- No backend changes in this ticket.

Implementation requirements
1) Env
- Ensure apps/web-user/.env.example includes:
  VITE_AUTOCOMPLETE_SERVICE_BASE_URL=http://localhost:<PORT>
  VITE_SEARCH_SERVICE_BASE_URL=http://localhost:8080
  VITE_QUERY_SERVICE_BASE_URL=http://localhost:8001
- Ensure .env is gitignored.

2) API client
- Update fetchAutocomplete() to resolve autocomplete base URL:
  - resolveAutocompleteBaseUrl() should prioritize:
    import.meta.env.VITE_AUTOCOMPLETE_SERVICE_BASE_URL
    then import.meta.env.VITE_AUTOCOMPLETE_BASE_URL (back-compat)
  - Build URL: `${base}/autocomplete?q=...&size=...`

3) UI wiring
- In the global search bar (AppShell or SearchPage, whichever owns the input):
  - Use useDebouncedValue(query, 200~300ms)
  - useEffect triggers fetchAutocomplete when debounced query length >= 1 (or 2)
  - Use AbortController to cancel previous request
  - Show dropdown suggestions
  - Clicking suggestion navigates to /search?q=<text>

4) Debugging ergonomics (small)
- If request fails, show a subtle inline message in the dropdown (no crash).
- Keep dependencies minimal.

Acceptance
- Running web-user on port 5174, typing in search input triggers GET requests to:
  http://localhost:<AUTOCOMPLETE_PORT>/autocomplete?q=...
- Suggestions render.
  Deliverables
- Changed files list + how to run locally + what to verify in DevTools.
