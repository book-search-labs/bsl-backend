Implement the ticket: tasks/backlog/T-0102-add-vanilla-vite-react-ts-web-apps-user-admin-env.md

Rules:
- Read and follow AGENTS.md.
- Stay within scope: ONLY modify apps/** and docs/RUNBOOK.md (if needed).
- Do NOT touch services/**, infra/**, contracts/**, db/**.

What to do:
1) Create two vanilla Vite (React + TS) apps:
  - apps/web-user/
  - apps/web-admin/
    Use the default Vite template only (no routing, no state libs, no UI kits, no tests).

2) Env wiring:
  - Add .env.example in each app with VITE_API_BASE_URL and VITE_SEARCH_BASE_URL defaults.
  - Ensure .env is gitignored (app-level .gitignore or root .gitignore update within scope).
  - Add minimal UI that prints env values and shows “Env loaded ✅ ” when both exist.

3) Docs:
  - Add apps/web-user/README.md and apps/web-admin/README.md with copy-paste run commands.
  - Optionally add a short section to docs/RUNBOOK.md linking to those READMEs.

Acceptance:
- `cd apps/web-user && npm i && cp .env.example .env && npm run dev -- --port 5173` works and shows env values.
- `cd apps/web-admin && npm i && cp .env.example .env && npm run dev -- --port 5174` works and shows env values.
- No real secrets committed.

Output:
- List created/updated files
- Copy-paste run commands for both apps
