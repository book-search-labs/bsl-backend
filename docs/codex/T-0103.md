You are Codex working in the bsl-frontend-admin repo.

Goal: Implement T-0103 “Admin Layout Shell + Router + Sidebar (MVP)” for apps/web-admin.

Read first (SSOT):
- apps/web-admin/README.md (if exists)
- apps/web-admin/.env.example
- apps/web-admin/src/App.tsx, src/main.tsx
- existing pages in apps/web-admin/src/pages/*

Constraints:
- Use the existing stack: Vite + React + TS + react-router-dom + react-bootstrap + bootstrap-icons.
- Do NOT add new dependencies.
- Keep changes scoped to apps/web-admin/** (plus README/.env.example within that app).
- No backend changes.

Deliverables:
1) Create a reusable layout shell:
  - Top header (dark) with left “도서검색 Admin” branding and right small label.
  - Left sidebar with collapsible menu groups:
    - Dashboard (children: Dashboard v1/v2/v3 routes)
    - Search Tools (children: Playground, Compare)
    - Ops (children: Index & Data -> Indices, Doc Lookup; Jobs)
    - Settings
  - Main content area renders route outlet.

2) Router setup:
  - Use react-router-dom with nested routes.
  - Define routes for existing pages:
    - /dashboard, /dashboard/v1, /dashboard/v2, /dashboard/v3 (you can reuse DashboardPage for v1~v3 as placeholders)
    - /tools/playground -> PlaygroundPage
    - /tools/compare -> ComparePage
    - /ops/index/indices -> placeholder page (simple “Indices” heading)
    - /ops/index/doc-lookup -> placeholder page
    - /ops/jobs -> placeholder page
    - /settings -> SettingsPage
  - Default route “/” should redirect to “/dashboard”.

3) Styling:
  - Use Bootstrap grid/flex utilities.
  - Sidebar fixed width (e.g., 280px), scrollable if long.
  - Main content has reasonable padding and uses container-fluid.
  - Active link styling (highlight current menu).

4) Environment:
  - Ensure bootstrap CSS and bootstrap-icons are imported once (main.tsx or App.tsx).
  - Update .env.example to include:
    - VITE_BSL_API_BASE_URL=http://localhost:8080
  - Update README.md with:
    - how to run dev server
    - env setup
    - route list (short)

Acceptance checks:
- `npm run dev` renders the layout and sidebar.
- Clicking menu items navigates without full page reload.
- Directly opening a nested URL (e.g., /tools/playground) works.
- No TypeScript errors; `npm run build` succeeds.

Output format:
- Provide a short summary of changed files and what was implemented.
