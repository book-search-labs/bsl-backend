Read tasks/backlog/T-0105-admin-search-playground-e2e.md and implement it.

Constraints:
- Only modify files under apps/web-admin/** (and docs/RUNBOOK.md only if the ticket explicitly allows it).
- Do not touch backend services/**, contracts/**, infra/**, db/**.
- Keep dependencies minimal (use existing react-router-dom + react-bootstrap + bootstrap).
- Add .env.example and ensure .env is ignored (do not commit secrets).

Deliverables:
- Admin UI has a Search Playground page that can call Search Service /search end-to-end.
- Supports toggling request mode: legacy vs qc.v1.1
- Shows results list + debug JSON (pretty printed)
- Basic UX: loading/error/empty states

Validation:
- `cd apps/web-admin && npm install && cp .env.example .env && npm run dev`
- Confirm calling http://localhost:8080/search works using VITE_SEARCH_BASE_URL.
- Provide curl-equivalent payload shown in the UI for both modes.
