import { Card } from "react-bootstrap";

export default function ComparePage() {
    return (
        <>
            <h3 className="mb-3">Compare</h3>
            <Card className="shadow-sm">
                <Card.Body>(A/B/C 결과 비교 UI를 여기에 붙이면 됨)</Card.Body>
            </Card>
        </>
    );
}